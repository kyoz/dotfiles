#!/usr/bin/env sh

case $BLOCK_BUTTON in
  1) $TERMINAL -e ibus-setup;;
esac

current_engine=$(ibus engine)

if [ $current_engine == "xkb:us::eng" ]; then
  lang="EN"
elif [ $current_engine == "teni" ]; then
  lang="VI"
else
  lang="EN"
fi

printf "%s" $lang
